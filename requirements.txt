# Build-pipeline dependencies.
#
# Derived from what the scripts actually import:
#   fetch_pages.py   -> markdownify, beautifulsoup4  (urllib, json, re are stdlib)
#   build_data.py    -> markdown        (re, glob, os, json, html, urllib, hashlib are stdlib)
#   build_images.py  -> Pillow          (json, os, re, io, base64, urllib, concurrent are stdlib)
#   build_site.py    -> stdlib only
#   build_urls.py    -> stdlib only
#
# Nothing else is needed. Content is read straight from the wiki's MediaWiki
# API, so there is no crawler service and no API key.

Markdown>=3.5,<4
Pillow>=10.0,<12
markdownify>=0.13,<2
beautifulsoup4>=4.12,<5
